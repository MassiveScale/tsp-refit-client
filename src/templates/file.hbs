{{!--
  file.hbs — outermost wrapper applied to every emitted C# file.

  Override via tspconfig.yaml:
    options:
      "tsp-refit-client":
        templates:
          file: "./templates/file.hbs"

  ── Template variables ──────────────────────────────────────────────────────

  {{namespace}}   — fully-qualified C# namespace string, e.g. "Acme.Api.Client"

  {{usings}}      — array of namespace strings (sorted: System.* first, then
                    alphabetically). Iterate with {{#each usings}}.
                    Does NOT include the `using` keyword or semicolons.

  {{body}}        — pre-rendered inner declaration produced by another template
                    (record.hbs, enum.hbs, or refit-interface.hbs).

  {{fileName}}    — basename of the file being emitted, e.g. "Widget.cs"

  ── Built-in helpers ────────────────────────────────────────────────────────

  {{#if (isDefined val)}}  — true when val is not undefined
  {{eq a b}}               — strict equality

--}}
//-----------------------------------------------------------------------
// <copyright file="{{fileName}}" />
//
// This file is AUTO-GENERATED. Do not edit this file directly.
// <auto-generated />
//-----------------------------------------------------------------------
#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member
#pragma warning disable CS0612 // Type or member is obsolete
#nullable enable
{{#each usings}}using {{this}};
{{/each}}

namespace {{namespace}};

{{body}}
#pragma warning restore CS0612 // Type or member is obsolete
#pragma warning restore CS1591 // Missing XML comment for publicly visible type or member